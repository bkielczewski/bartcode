language: java

jdk:
  - oraclejdk8

build:
  cache: true

  ci:
   - mvn clean deploy -Dbranch=${BRANCH} -Dbuild.number=${BUILD_NUMBER} -q

integrations:
    hub:
      - integrationName: docker
        type: "Docker"
