language: java

jdk:
  - openjdk8

build:
  cache: true

  ci:
    - ./shippable_ci.sh

  post_ci:
    - ./shippable_post_ci.sh bkielczewski

integrations:
  hub:
    - integrationName: docker
      type: "Docker"
